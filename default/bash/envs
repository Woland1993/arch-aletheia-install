# Editor used by CLI
export SUDO_EDITOR="$EDITOR"
export BAT_THEME=ansi

# Duplicated from .config/uwsm/env so SSH works too
export ALETHEIA_PATH=$HOME/.local/share/aletheia
export PATH=$ALETHEIA_PATH/bin:$PATH:$HOME/.local/bin
