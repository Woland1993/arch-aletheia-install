#!/bin/bash

# Set the branch for Aletheia's git repository.

if (($# == 0)); then
  echo "Usage: aletheia-branch-set [master|rc|dev]"
  exit 1
else
  branch="$1"
fi

if [[ $branch != "master" && $branch != "rc" && $branch != "dev" ]]; then
  echo "Error: Invalid branch '$branch'. Must be one of: master, rc, dev"
  exit 1
fi

git -C $ALETHEIA_PATH switch $branch
