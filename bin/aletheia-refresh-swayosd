#!/bin/bash

# Overwrite the user configs for swayosd (controls on-screen feedback for changing volume/songs etc) with the Aletheia defaults and restart the service.

aletheia-refresh-config swayosd/config.toml
aletheia-refresh-config swayosd/style.css
aletheia-restart-swayosd
