#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/aletheia/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  aletheia-theme-set "$(cat $THEME_NAME_PATH)"
fi
